# 北方汇生活本地开发 Redis 配置，仅允许本机访问。
bind 127.0.0.1 ::1
protected-mode yes
port 6379
timeout 0
tcp-keepalive 300
databases 16

# 同时保留快照和 AOF，避免 Windows 或 WSL 异常退出后丢失会话。
save 900 1
save 300 10
save 60 10000
appendonly yes
appendfsync everysec

# 会话数据不允许因内存淘汰而无故失效。
maxmemory-policy noeviction
